# 更新记录

## 未发布

- 增加 ClickHouse HNSW 向量存储与权限感知检索，FAISS 作为可配置的开发降级方案。
- 增加不可变索引快照、30 天旧快照 TTL 和 ClickHouse 强制可用模式。
- 增加 ClickHouse Docker 部署、迁移脚本、健康检查及连接配置。
- 增加管理员模型服务页面，支持 vLLM、OpenRouter 和其他 OpenAI-compatible API。
- 模型 API Key 加密保存且不回显，支持连接测试、主机白名单和运行时切换。
- 增加答案“有用/没用”反馈及管理员反馈统计。
- 增加近 14 天提问趋势、未命中检索列表和审计日志 CSV 导出。
- 评测题支持编辑、启停与删除，便于维护回归测试集。
- 员工列表支持快速编辑、停用与重新启用账号。
- 资料库支持选择两个版本查看逐行差异。
- 增加管理员运行看板、完整操作审计和 RAG 标准问题评测页面。
- 评测仅执行检索与重排，不调用生成模型，并记录命中率和耗时。
- 检索升级为 BM25 关键词、BGE 向量、RRF 融合和 Reranker 的混合检索。
- 改进切片边界并增加上下文重叠，保留 Excel 短行和短条款。
- 增加 SQLite 增量向量缓存，未变化内容无需重复计算 Embedding。
- 增加低相关度拒答阈值和不保存问题原文的检索指标记录。
- 增加网页资料库，可按用户权限浏览、搜索和预览资料摘要。
- 管理员可在资料库中修改资料的可见范围和授权部门。
- 管理员可在二次确认后删除资料；删除后同步移出检索索引。
- 增加多用户、管理员角色、部门管理和员工账号管理。
- 增加资料级全公司、指定部门、仅管理员三种访问范围，并在向量检索前强制过滤。
- 增加完整版本与增量修订识别，按生效顺序组成当前有效版本链。
- 增加历史年份版本链重建，并让后续修订优先于冲突的旧内容。
- 增加可视化“添加资料”弹窗，可选择文件、WPS/金山、腾讯文档和云存储。
- 增加网页文件上传与上传后自动重建索引。
- 增加 S3 兼容云端资料库，支持 COS、OSS、S3 和 MinIO。
- 在线平台密钥加密保存，列表接口不返回密钥原文。
- 增加 WPS/金山文档内容抽取连接器与 KSO-1 签名。
- 增加腾讯文档开放平台可配置连接器。
- 增加在线文档定时同步和网页手动同步按钮。
- 在线同步失败时保留上一版可用索引。

## v1.0.0 — 第一版（2026-08-24）

- 完成独立企业知识库网页与 FastAPI 后端。
- 支持 PDF、Word、Excel、文本和图片 OCR。
- 完成 BGE、FAISS、Reranker Top 3 检索链路。
- 接入 OpenAI-compatible vLLM / Qwen 问答。
- 增加答案来源定位和资料不足保护。
- 增加同系列文档最新版本及指定年份判断。
- 增加登录验证、密码哈希与失败限流。
- 提供 Docker、WSL 和 Linux 部署基础。
